/*
package com.example.codenames.socketNetwork;

import com.example.codenames.GameEngine.GameRoles;

import java.io.IOException;
import java.net.ServerSocket;

public class HostTest {
    public static void main(String[] args) throws IOException, InterruptedException {
        Server host = new Host("Karl", GameRoles.DETECTIVE,new ServerSocket(8888));
        host.start();

        // Kommunkation vom Server mit Client -> siehe SocketHandler
    }
}

 */
